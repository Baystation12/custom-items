# dmi icon merger hook
# needs additional setup, see tools/dmitool/merging.txt
*.dmi merge=merge-dmi

# force config merging to use union
# doesn't work so well at the moment
# config/custom_items.txt merge=union
# config/custom_sprites.txt merge=union
